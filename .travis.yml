language: python

python:
  - 2.7
  - 3.6

install:
  - pip install .

script:
  - python setup.py test          

after_success: 
  - pip install --user travis-sphinx
  - travis-sphinx build
  - travis-sphinx deploy
